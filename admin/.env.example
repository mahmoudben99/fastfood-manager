# Copy this to .env.local and fill in your values
# NEVER commit .env.local to git

# Supabase project URL (safe to share)
SUPABASE_URL=https://ijdiiixkemrmkhhkbcng.supabase.co

# Supabase service_role key — KEEP SECRET, add to Vercel env vars only
# Get from: Supabase -> Settings -> API -> service_role (secret) key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Admin dashboard login password (your choice)
ADMIN_PASSWORD=your_secret_admin_password

# Random 32+ char string for session cookie signing
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=replace_with_random_32_char_string

# Same secret keys as in src/main/activation/activation.ts
FFM_SECRET_KEY=FFM-2024-SERIAL-KEY-DO-NOT-SHARE
FFM_UNLOCK_KEY=FFM-2024-UNLOCK-KEY-DO-NOT-SHARE
